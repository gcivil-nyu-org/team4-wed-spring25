# .ebextensions/00_enable_epel.config
commands:
  01_enable_epel:
    # Command for Amazon Linux 2
    command: "sudo amazon-linux-extras install epel -y"
    # Test to avoid running repeatedly, adjust or remove as needed
    test: "! sudo yum repolist enabled | grep -q epel/"